# Unreal Engine 5.8 Upgrade Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the customized Cesium for Unreal fork compile and pass relevant tests with Unreal Engine 5.8.

**Architecture:** Selectively apply the source-level compatibility delta from upstream merge commit `28c40e8d94cd537f04cb6e93412c9cc4d5ce3869` on top of the customized fork. Preserve local Runtime Nanite and Holo behavior, use a UE 5.8 host build as the initial failing test, and integrate native changes only when the build proves they are required.

**Tech Stack:** Unreal Engine 5.8, Unreal Build Tool, C++20, Git submodules, Cesium automation tests

---

### Task 1: Establish the isolated baseline

**Files:**
- Inspect: `Source/CesiumRuntime/**`
- Inspect: `Source/CesiumEditor/**`
- Inspect: `extern/cesium-native`

- [ ] **Step 1: Confirm isolation and record the main worktree state**

Run `git status --short --branch` in both the main worktree and this worktree.
Expected: the upgrade worktree is clean; the main worktree retains its existing modified and untracked files.

- [ ] **Step 2: Initialize the worktree submodules**

Run `git submodule update --init --recursive`.
Expected: the committed customized `cesium-native` revision is checked out. If an unavailable nested revision prevents recursion, initialize the top-level submodule first and diagnose that nested dependency separately.

- [ ] **Step 3: Locate or prepare the UE 5.8 host project**

Inspect `C:\tmp\cesium-runtime-nanite-host\HostProject.uproject` and its plugin linkage. Create a separate UE 5.8 host under `C:\tmp` only if the existing host cannot point safely at this worktree.
Expected: the host references `E:\UnrealProjects\cesium-unreal\.worktrees\ue58-upgrade` and declares EngineAssociation `5.8`.

### Task 2: Capture the UE 5.8 compilation failure

**Files:**
- Test: `C:\tmp\cesium-runtime-nanite-host\HostProject.uproject`
- Inspect: `Source/CesiumRuntime/CesiumRuntime.Build.cs`

- [ ] **Step 1: Build the unmodified fork with UE 5.8**

Run the UE 5.8 `Build.bat` for the host Editor target.
Expected: FAIL on one or more UE 5.8 API or compiler compatibility errors, providing the RED phase. Save the complete build log under `C:\tmp`.

- [ ] **Step 2: Classify every failure**

Map failures to upstream compatibility edits, customized Runtime Nanite interactions, native dependencies, or unrelated host configuration.
Expected: each compiler error has an identified owner and proposed minimal change.

### Task 3: Apply the selected upstream compatibility delta

**Files:**
- Modify: `Documentation/Source/dev.Target.cs`
- Modify: `Documentation/Source/devEditor.Target.cs`
- Modify: `Source/CesiumEditor/Private/CesiumFeaturesMetadataViewer.cpp`
- Modify: `Source/CesiumEditor/Private/CesiumVoxelShaderBuilder.cpp`
- Modify: `Source/CesiumRuntime/Private/Cesium3DTileset.cpp`
- Modify: `Source/CesiumRuntime/Private/CesiumGaussianSplatSubsystem.cpp`
- Modify: `Source/CesiumRuntime/Private/CesiumGltfComponent.cpp`
- Modify: `Source/CesiumRuntime/Private/CesiumTextureResource.cpp`
- Modify: `Source/CesiumRuntime/Private/CesiumViewExtension.cpp`
- Modify: `Source/CesiumRuntime/Private/VoxelMegatextures.cpp`
- Modify: `Source/CesiumRuntime/Private/VoxelOctree.cpp`
- Modify: `Source/CesiumRuntime/Public/CesiumCommon.h`
- Modify: relevant float-literal test files under `Source/CesiumRuntime/Private/Tests/`

- [ ] **Step 1: Generate a source-only patch from the upstream merge**

Generate the first-parent diff from `23fde6cb` to `28c40e8d` limited to `Documentation/Source`, `Source/CesiumEditor`, and `Source/CesiumRuntime`.
Expected: no `.github`, maintainer documentation, assets, or submodule gitlink changes are included.

- [ ] **Step 2: Apply the patch with three-way context**

Apply with `git apply --3way`, resolving overlap by preserving customized behavior and introducing only the UE 5.8 API change.
Expected: the selected upstream delta is present and Runtime Nanite/Holo files remain otherwise unchanged.

- [ ] **Step 3: Review the resulting diff**

Compare every changed hunk with the upstream commit and inspect for accidental formatting or unrelated edits.
Expected: only UE 5.8 compatibility changes remain.

- [ ] **Step 4: Commit the compatibility port**

Commit as `feat: add Unreal Engine 5.8 source compatibility`.

### Task 4: Reach a clean UE 5.8 build

**Files:**
- Modify: only files named by concrete UE 5.8 compiler failures
- Test: host Editor target

- [ ] **Step 1: Rebuild with UE 5.8**

Run the same host Editor build command.
Expected: either PASS or a smaller, newly actionable failure set.

- [ ] **Step 2: Fix fork-specific failures one at a time**

For behavioral defects, add or select an automation test that reproduces the defect before editing production code. For compile-only API migrations, the failing compile is the regression test.
Expected: each change directly eliminates its corresponding failure without suppressing warnings globally.

- [ ] **Step 3: Repeat until the Editor target builds**

Expected: UE 5.8 Build Tool exits with code 0.

- [ ] **Step 4: Commit any additional fixes**

Commit focused fixes separately from the upstream compatibility port.

### Task 5: Validate Cesium behavior and preserve local state

**Files:**
- Test: `Source/CesiumRuntime/Private/Tests/**`
- Inspect: main and upgrade worktree status

- [ ] **Step 1: Run relevant Cesium automation tests under UE 5.8**

Run Runtime Nanite tests plus tests associated with any modified runtime subsystem.
Expected: all selected tests pass with zero failures.

- [ ] **Step 2: Audit the final branch diff**

Run `git diff 00e1be08...HEAD --stat` and inspect the full diff.
Expected: no CI configuration, upstream maintainer guide, or unwanted `cesium-native` pointer replacement.

- [ ] **Step 3: Verify the original worktree is unchanged**

Compare its status with the Task 1 snapshot.
Expected: all pre-existing tracked and untracked user files remain intact.

- [ ] **Step 4: Run final verification and report integration options**

Re-run the UE 5.8 Editor build and selected tests from a clean state. Report the branch, commits, commands, and exact results before offering merge or cherry-pick into `main`.
